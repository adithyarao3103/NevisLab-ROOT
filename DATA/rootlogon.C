// The contents of this file will be executed at the beginning of any
// interactive ROOT session that starts in this directory.
{
  // Uncomment this list to load the ExampleEvents library, which is
  // handy if we want to read the file exampleEvents.root directly.
  // If we're not reading that particular file, then we don't need the
  // following line.
  // 22-May-2015: Since I created an ExampleEvent.rootmap file,
  // I don't need this line anymore.
  //gSystem->Load("libExampleEvent.so");

  // Uncomment this line to load a ROOT browser window every time you
  // start ROOT.
  // TBrowser tb;
  return;
}
